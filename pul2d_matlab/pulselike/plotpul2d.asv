%% plot

a=0.5;
b=5;
d=0;
alpha=1;

m=50;
dx=b/m;
q=(-b):dx:b;

load('v_pul2da'+string(a)+'b'+string(b)+'d'+string(d)+'amp'+string(alpha)+'.mat')
for i=1:N
  for j=1:N
    r=q(i)^2+q(j)^2;
    if ((r>=a2)&(r<=b2))
    V(i,j)=mod(U(i,j)-U(i0,j0),2*pi);
    V0(i,j)=mod(U0(i,j)-U0(i0,j0),2*pi);
    end
  end
end

figure
h=pcolor(q,q,V);
colormap('hsv');
set(h,'EdgeColor','none');
